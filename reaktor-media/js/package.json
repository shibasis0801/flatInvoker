{
  "name": "mehmaan-image",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "../../gradlew :lib-media:webNodeProductionLibraryDistribution",
    "deploy": "yarn build && wrangler deploy",
    "start": "yarn build && wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230419.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  }
}
